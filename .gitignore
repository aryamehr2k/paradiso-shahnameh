# --- environments ---
.venv/
__pycache__/
*.pyc

# --- third-party trainer (cloned by setup_gb10.sh) ---
ai-toolkit/

# --- data & training artifacts (regenerated by the pipeline) ---
dataset/
output/

# --- model weights / checkpoints (NEVER commit) ---
*.safetensors
*.pt
*.ckpt
*.bin

# --- Hugging Face tokens / caches (NEVER commit) ---
.huggingface/
huggingface/
*.hf_token
token
.env

# --- logs / run scratch ---
*.log
*.pid

# --- generated renders at repo root (curated copies live in samples/) ---
/01_river_of_light.png
/02_river_becomes_rose.png
/03_celestial_rose.png
